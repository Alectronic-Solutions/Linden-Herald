## What changed

<!-- One or two sentences. -->

## Checks

- [ ] `npm run check` passes (lint, typecheck, src/data validation)
- [ ] `npm run build` passes
- [ ] If this touched a price, deadline or fact about the paper, `VERIFY.md` is updated
- [ ] If this added an issue, the PDF is in `public/issues/` and `npm run validate` passes
